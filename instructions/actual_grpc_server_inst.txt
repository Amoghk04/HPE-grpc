The web page works only on MACOSX and Debian-based Linux systems, so if you dont have any of these, make sure to either dual boot or install a VM
steps to run the acual grpc server - 
follow all the steps in week3.txt for creating the server

----------------------------------------------------------------------------------------------------------------------------------------
1. add the certs folder inside the root directory, not inside the grpc directory

----------------------------------------------------------------------------------------------------------------------------------------
2. commands to create the certificates
```
mkdir -p certs
cd certs

echo "Generating SSL certificates without passphrase..."

# 1. Generate CA key and certificate
echo "Generating CA certificates..."
openssl genrsa -out ca.key 2048
openssl req -new -x509 -key ca.key -out ca.crt -days 365 \
    -subj "/C=US/ST=California/L=SanFrancisco/O=MyOrg/CN=MyCA"

# 2. Generate Server key and certificate
echo "Generating Server certificates..."
openssl genrsa -out server.key 2048
openssl req -new -key server.key -out server.csr \
    -subj "/C=US/ST=California/L=SanFrancisco/O=MyOrg/CN=localhost"
openssl x509 -req -days 365 -in server.csr -CA ca.crt -CAkey ca.key -set_serial 01 -out server.crt

# 3. Generate Client key and certificate
echo "Generating Client certificates..."
openssl genrsa -out client.key 2048
openssl req -new -key client.key -out client.csr \
    -subj "/C=US/ST=California/L=SanFrancisco/O=MyOrg/CN=client"
openssl x509 -req -days 365 -in client.csr -CA ca.crt -CAkey ca.key -set_serial 02 -out client.crt

# Set correct permissions
chmod 644 *.crt
chmod 644 *.key

echo "Certificate generation complete!"
echo "Verifying certificates..."
openssl verify -CAfile ca.crt server.crt
openssl verify -CAfile ca.crt client.crt

cd ..
```

----------------------------------------------------------------------------------------------------------------------------------------
3. Download and install envoy - https://www.envoyproxy.io/docs/envoy/latest/start/start

----------------------------------------------------------------------------------------------------------------------------------------
4. Generate the proto files (for both server and webUI)
``` npm install -g protoc-gen-grpc-web
for the grpc server side follow the original protoc command in the readme file
for the webUI run the below command
protoc -I=. \
    --js_out=import_style=commonjs:../web/src \
    --grpc-web_out=import_style=commonjs,mode=grpcwebtext:../web/src \
    service.proto
----------------------------------------------------------------------------------------------------------------------------------------
5. run these commands inside the web directory
npm install --save google-protobuf grpc-web webpack webpack-cli
npm install --save-dev @grpc/grpc-js @grpc/proto-loader

----------------------------------------------------------------------------------------------------------------------------------------
6. open 3 terminals
1st terminal(inside grpc/build) - mkdir -p uploads downloads 
then run the server - ./server

2nd terminal(root directory) - envoy -c envoy.yaml --mode validate, it should print OK, at the end
run envoy -c envoy.yaml 

3rd terminal(web directory) - run npm install -> npm start

The web app must be running if all the steps are followed properly.
----------------------------------------------------------------------------------------------------------------------------------------
